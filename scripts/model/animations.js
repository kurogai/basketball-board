const a =

    // obj -> id do objecto dentro da janela
    // message -> texto a ser apresentado pra mensagem
    // type -> tipo de mensagem (alerta, informativo, erro, etc)
    function spawn_message(obj,message,type) {
        
    }